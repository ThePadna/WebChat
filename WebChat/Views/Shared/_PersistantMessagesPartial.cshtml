@model IndexViewModel
@{
    foreach (var m in Model._messageList)
    {
        <script>
                window.onload = function () {
                    $('#messages').append("[@m.Date] " + "@m.Sender" + ": " + "@m.Contents")
                };
        </script>
    }
}
